<div class="flex flex-col gap-8 w-full">
  <!-- Title Header Row -->
  <div class="flex justify-between items-start flex-wrap gap-4">
    <div>
      <div class="flex items-center gap-2 text-xs text-gray-500 font-semibold uppercase tracking-wider mb-1">
        <%= link_to "Dashboard", dashboard_path, class: "hover:text-white" %>
        <span>/</span>
        <span class="text-white">Projects</span>
      </div>
      <h1 class="text-3xl font-extrabold tracking-tight text-white"><%= @project.name %></h1>
      <p class="text-sm text-gray-400">Map and group supply chain trust registries under this project environment.</p>
    </div>
    <div class="flex gap-3">
      <%= button_to "Delete Project", project_path(@project), method: :delete, data: { confirm: "Are you sure you want to permanently delete this project?" }, class: "px-4 py-2 text-xs font-bold rounded-lg border border-[#ef4444]/30 bg-[#ef4444]/10 text-[#ef4444] cursor-pointer hover:bg-[#ef4444] hover:text-black transition-all uppercase tracking-wider" %>
      <%= link_to "Back to Dashboard", dashboard_path, class: "px-4 py-2 text-xs font-bold rounded-lg border border-[#1f2937] text-gray-400 hover:text-white hover:border-gray-500 transition-colors uppercase tracking-wider flex items-center justify-center" %>
    </div>
  </div>

  <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
    <!-- Left: Linked Repositories (2 Cols wide) -->
    <div class="lg:col-span-2 flex flex-col gap-6">
      <div class="border border-[#1f2937] rounded-xl bg-[#111827]/20 p-6 flex flex-col gap-6">
        <div>
          <h2 class="text-xl font-bold text-white">Linked Registries</h2>
          <p class="text-xs text-gray-400">Repositories associated with this environment container.</p>
        </div>

        <% if @linked_repositories.empty? %>
          <div class="p-8 text-center border border-dashed border-[#1f2937] rounded-xl flex flex-col gap-3">
            <span class="text-xl">🔗</span>
            <p class="text-sm text-gray-500">No repositories linked to this project container yet.</p>
          </div>
        <% else %>
          <div class="flex flex-col gap-4">
            <% @linked_repositories.each do |repo| %>
              <div class="p-5 rounded-lg border border-[#1f2937] bg-[#0b0f19] flex justify-between items-center gap-4 hover:border-gray-500 transition-all">
                <div>
                  <%= link_to "#{repo['owner']}/#{repo['repo']}", repository_path(repo['id']), class: "font-mono font-bold text-sm text-[#10b981] hover:underline" %>
                  <div class="text-[10px] text-gray-500 font-semibold mt-1 flex items-center gap-2">
                    <span>ID: <%= repo["id"] %></span>
                    <span>•</span>
                    <% if repo["is_premium"] == 1 %>
                      <span class="text-[#8b5cf6] font-bold">PREMIUM TIER</span>
                    <% else %>
                      <span class="text-gray-400">STANDARD TIER</span>
                    <% end %>
                  </div>
                </div>
                
                <%= button_to "Unlink Repository", unlink_repository_project_path(@project, repo_id: repo["id"]), method: :delete, class: "px-3 py-1.5 text-[10px] font-bold rounded border border-[#ef4444]/30 bg-[#ef4444]/10 text-[#ef4444] cursor-pointer hover:bg-[#ef4444] hover:text-black transition-all uppercase tracking-wider" %>
              </div>
            <% end %>
          </div>
        <% end %>
      </div>

      <!-- Candlestick Charts Section -->
      <% unless @linked_repositories.empty? %>
        <% @linked_repositories.each do |repo| %>
          <div class="border border-[#1f2937] rounded-xl bg-[#111827]/20 p-6 flex flex-col gap-4 mt-6">
            <div class="flex justify-between items-start flex-wrap gap-4">
              <div>
                <h3 class="text-lg font-bold text-white">SemVer Candlestick Timelines: <%= repo['owner'] %>/<%= repo['repo'] %></h3>
                <p class="text-xs text-gray-400">Visualizes package constraints (wick), active version timeline (body), current pinned version (dot), and upstream release drift (line/endpoint).</p>
              </div>
              <% if repo['is_premium'] != 1 %>
                <span class="text-[10px] font-bold px-2 py-0.5 rounded border border-[#8b5cf6]/30 bg-[#8b5cf6]/10 text-[#8b5cf6] uppercase">Standard Tier</span>
              <% else %>
                <span class="text-[10px] font-bold px-2 py-0.5 rounded border border-[#10b981]/30 bg-[#10b981]/10 text-[#10b981] uppercase">Premium Tier</span>
              <% end %>
            </div>

            <% if repo['is_premium'] != 1 %>
              <div class="p-3.5 rounded-lg border border-[#8b5cf6]/20 bg-[#8b5cf6]/5 text-xs text-[#c084fc] flex items-center gap-2">
                <span class="text-base">💡</span>
                <span><strong>Hosted Registry Customers Only:</strong> Upstream release monitoring and drift warning alerts are restricted to premium accounts. Upgrade this repository to Premium to enable real-time SemVer drift detection.</span>
              </div>
            <% end %>
            
            <div id="candlestick-chart-<%= repo['id'] %>" data-url="<%= candlesticks_repository_path(repo['id']) %>" data-premium="<%= repo['is_premium'] == 1 %>" class="candlestick-chart w-full bg-[#0b0f19]/30 rounded-lg p-4 border border-[#1f2937]/50 min-h-[100px] flex flex-col gap-6 items-stretch">
              <div class="flex items-center justify-center py-8 loader-placeholder">
                <span class="text-xs text-gray-500 italic">Loading candlesticks...</span>
              </div>
            </div>
          </div>
        <% end %>
      <% end %>
    </div>

    <!-- Right: Linking Tool (1 Col wide) -->
    <div class="flex flex-col gap-6">
      <div class="border border-[#1f2937] rounded-xl bg-[#111827]/20 p-6 flex flex-col gap-6">
        <div>
          <h2 class="text-xl font-bold text-white">Registry Linker Tool</h2>
          <p class="text-xs text-gray-400">Add active package ledgers to this project space.</p>
        </div>

        <% if @unlinked_repositories.empty? %>
          <p class="text-sm text-gray-500 italic py-2">All registered repository ledgers are already mapped to this environment or another project.</p>
        <% else %>
          <%= form_with(url: link_repository_project_path(@project), method: :post, class: "flex flex-col gap-4") do |form| %>
            <div class="flex flex-col gap-2">
              <%= form.label :repo_id, "Select Unlinked Repository", class: "text-xs font-semibold text-gray-400 uppercase tracking-wider" %>
              <%= form.select :repo_id, options_for_select(@unlinked_repositories.map { |r| ["#{r['owner']}/#{r['repo']} (ID: #{r['id']})", r["id"]] }), {}, class: "w-full bg-[#0b0f19] border border-[#1f2937] rounded-lg px-4 py-3 text-sm text-white focus:outline-none focus:border-[#10b981] transition-colors" %>
            </div>
            <%= form.submit "Link Selected Repository", class: "w-full py-3 rounded-lg bg-[#10b981] text-black font-bold text-xs cursor-pointer hover:bg-[#10b981]/90 transition-all uppercase tracking-wider" %>
          <% end %>
        <% end %>
      </div>
    </div>
  </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/d3@7"></script>
<script src="https://cdn.jsdelivr.net/npm/js-yaml@4.1.0/dist/js-yaml.min.js"></script>
<script>
  document.addEventListener("turbo:load", initCandlesticks);

  if (document.readyState === "complete" || document.readyState === "interactive") {
    initCandlesticks();
  } else {
    document.addEventListener("DOMContentLoaded", initCandlesticks);
  }

  function initCandlesticks() {
    const charts = document.querySelectorAll(".candlestick-chart");
    charts.forEach(container => {
      if (container.getAttribute("data-initialized") === "true") return;
      container.setAttribute("data-initialized", "true");

      const url = container.getAttribute("data-url");
      const isPremium = container.getAttribute("data-premium") === "true";
      fetch(url)
        .then(res => {
          if (!res.ok) throw new Error("Status: " + res.status);
          return res.text();
        })
        .then(yamlText => {
          const data = jsyaml.load(yamlText);
          renderChart(container, data, isPremium);
        })
        .catch(err => {
          container.innerHTML = `<span class="text-xs text-red-500 font-semibold">Failed to load candlesticks: ${err.message}</span>`;
        });
    });
  }

  function semverToNumber(ver) {
    if (!ver || ver === "infinity") return 0;
    const clean = ver.replace(/[^0-9.]/g, "").trim();
    if (!clean) return 0;
    const parts = clean.split(".").map(x => {
      const p = parseInt(x, 10);
      return isNaN(p) ? 0 : p;
    });
    while (parts.length < 3) {
      parts.push(0);
    }
    return parts[0] * 1000000 + parts[1] * 1000 + parts[2];
  }

  function renderChart(container, data, isPremium) {
    container.innerHTML = ""; // Clear loader
    if (!data || data.length === 0) {
      container.innerHTML = `<div class="flex items-center justify-center py-8"><span class="text-xs text-gray-500 italic">No package SemVer constraints found.</span></div>`;
      return;
    }

    // Group packages by manifest
    const groups = {};
    data.forEach(d => {
      const m = d.manifest || "package.json";
      if (!groups[m]) groups[m] = [];
      groups[m].push(d);
    });

    // Render a section for each manifest
    Object.entries(groups).forEach(([manifestName, groupData]) => {
      const subContainer = document.createElement("div");
      subContainer.className = "mb-8 border-b border-[#1f2937]/30 pb-6 last:border-0 last:pb-0";

      const title = document.createElement("h4");
      title.className = "text-xs font-semibold text-gray-400 mb-4 font-mono bg-[#111827]/40 px-3 py-2 rounded-lg border border-[#1f2937]/50 inline-block";
      title.innerText = `Manifest: ${manifestName}`;
      subContainer.appendChild(title);

      const chartDiv = document.createElement("div");
      chartDiv.className = "w-full";
      subContainer.appendChild(chartDiv);
      container.appendChild(subContainer);

      const actualPackages = groupData.filter(d => d.package);
      if (actualPackages.length === 0) {
        chartDiv.innerHTML = `<span class="text-xs text-gray-500 italic block pl-4 pb-2">No package constraints tracked in this manifest.</span>`;
      } else {
        renderSingleChart(chartDiv, actualPackages, isPremium);
      }
    });
  }

  function renderSingleChart(container, data, isPremium) {
    const rowHeight = 60;
    const margin = { top: 15, right: 80, bottom: 25, left: 160 };
    // Ensure chartWidth is always at least 600px
    const chartWidth = Math.max(600, container.clientWidth || 600);
    const width = chartWidth - margin.left - margin.right;
    const height = data.length * rowHeight;

    let tooltip = d3.select("body").select(".candlestick-tooltip");
    if (tooltip.empty()) {
      tooltip = d3.select("body")
        .append("div")
        .attr("class", "candlestick-tooltip")
        .style("position", "absolute")
        .style("visibility", "hidden")
        .style("background", "rgba(11, 15, 25, 0.95)")
        .style("border", "1px solid rgba(255, 255, 255, 0.15)")
        .style("padding", "14px 18px")
        .style("border-radius", "8px")
        .style("box-shadow", "0 10px 30px -5px rgba(0, 0, 0, 0.7), 0 8px 16px -8px rgba(0, 0, 0, 0.7)")
        .style("color", "#cbd5e1")
        .style("font-family", "ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace")
        .style("font-size", "12px")
        .style("pointer-events", "none")
        .style("z-index", "9999")
        .style("backdrop-filter", "blur(12px)")
        .style("transition", "opacity 0.15s ease-out, transform 0.15s ease-out")
        .style("opacity", "0");
    }

    const svg = d3.select(container)
      .append("svg")
      .attr("width", chartWidth)
      .attr("height", height + margin.top + margin.bottom)
      .append("g")
      .attr("transform", `translate(${margin.left}, ${margin.top})`);

    data.forEach((d, idx) => {
      const y = idx * rowHeight + rowHeight / 2;

      const minVal = semverToNumber(d.min_version) || 0;
      const firstVal = semverToNumber(d.first_seen_version) || minVal;
      const pinnedVal = semverToNumber(d.current_pinned_version) || minVal;
      const latestVal = semverToNumber(d.latest_upstream_version) || pinnedVal;
      const isInfinity = d.max_version === "infinity";

      let maxVal = isInfinity
        ? Math.max(latestVal, pinnedVal) + (Math.max(latestVal, pinnedVal) - minVal || 1000) * 1.5
        : (semverToNumber(d.max_version) || (Math.max(minVal, pinnedVal, latestVal) + 1000));
      if (maxVal <= minVal) maxVal = minVal + 1000;

      const domainStart = Math.min(minVal, firstVal);
      const xScale = d3.scaleLinear()
        .domain([domainStart, maxVal])
        .range([0, width]);

      const g = svg.append("g");

      if (idx < data.length - 1) {
        svg.append("line")
          .attr("x1", -margin.left)
          .attr("y1", (idx + 1) * rowHeight)
          .attr("x2", width + margin.right)
          .attr("y2", (idx + 1) * rowHeight)
          .attr("stroke", "#1f2937")
          .attr("stroke-width", 1);
      }

      g.append("text")
        .attr("x", -10)
        .attr("y", y + 4)
        .attr("text-anchor", "end")
        .attr("fill", "#ffffff")
        .attr("font-size", "13px")
        .attr("font-family", "monospace")
        .attr("font-weight", "bold")
        .text(d.package);

      g.append("text")
        .attr("x", -10)
        .attr("y", y + 16)
        .attr("text-anchor", "end")
        .attr("fill", "#cbd5e1")
        .attr("font-size", "11px")
        .attr("font-family", "monospace")
        .text(`(${d.constraint})`);

      const xMin = xScale(minVal);
      const xMax = isInfinity ? width : xScale(maxVal);

      g.append("line")
        .attr("x1", xMin)
        .attr("y1", y)
        .attr("x2", xMax)
        .attr("y2", y)
        .attr("stroke", "#4b5563")
        .attr("stroke-dasharray", isInfinity ? "4,4" : "none")
        .attr("stroke-width", 2);

      g.append("line")
        .attr("x1", xMin)
        .attr("y1", y - 6)
        .attr("x2", xMin)
        .attr("y2", y + 6)
        .attr("stroke", "#4b5563")
        .attr("stroke-width", 2);

      if (!isInfinity) {
        g.append("line")
          .attr("x1", xMax)
          .attr("y1", y - 6)
          .attr("x2", xMax)
          .attr("y2", y + 6)
          .attr("stroke", "#4b5563")
          .attr("stroke-width", 2);
      } else {
        g.append("text")
          .attr("x", xMax + 10)
          .attr("y", y + 4)
          .attr("fill", "#9ca3af")
          .attr("font-size", "14px")
          .text("∞");
      }

      const xFirst = xScale(firstVal);
      const xLatest = xScale(latestVal);

      const bodyMin = Math.min(xFirst, xLatest);
      const bodyMax = Math.max(xFirst, xLatest);
      const bodyWidth = Math.max(2, bodyMax - bodyMin);

      const hasDrift = isPremium ? (pinnedVal < latestVal) : false;
      const bodyColor = hasDrift ? "#f59e0b" : "#10b981";
      const driftColor = hasDrift ? "#f59e0b" : "#10b981";
      const driftText = hasDrift ? "Drift Detected" : "Up to Date";

      const tooltipHtml = isPremium ? `
        <div style="font-weight: bold; font-size: 13px; color: #ffffff; margin-bottom: 8px; border-bottom: 1px solid rgba(255, 255, 255, 0.1); padding-bottom: 6px; font-family: sans-serif;">
          ${d.package} <span style="font-weight: normal; color: #9ca3af; font-size: 11px;">(${d.constraint})</span>
        </div>
        <table style="border-collapse: collapse; width: 100%; font-size: 11px; font-family: monospace;">
          <tr>
            <td style="color: #9ca3af; padding: 2px 8px 2px 0; text-align: left;">Pinned:</td>
            <td style="color: #6366f1; font-weight: bold; padding: 2px 0;">${d.current_pinned_version || 'N/A'}</td>
          </tr>
          <tr>
            <td style="color: #9ca3af; padding: 2px 8px 2px 0; text-align: left;">First Seen:</td>
            <td style="color: #e2e8f0; padding: 2px 0;">${d.first_seen_version || 'N/A'}</td>
          </tr>
          <tr>
            <td style="color: #9ca3af; padding: 2px 8px 2px 0; text-align: left;">Latest Upstream:</td>
            <td style="color: #e2e8f0; padding: 2px 0;">${d.latest_upstream_version || 'N/A'}</td>
          </tr>
          <tr>
            <td style="color: #9ca3af; padding: 2px 8px 2px 0; text-align: left;">Allowed Range:</td>
            <td style="color: #cbd5e1; padding: 2px 0;">${d.min_version || '0.0.0'} to ${d.max_version || '∞'}</td>
          </tr>
          <tr>
            <td style="color: #9ca3af; padding: 6px 8px 2px 0; text-align: left;">Status:</td>
            <td style="padding: 6px 0;">
              <span style="background-color: ${hasDrift ? 'rgba(245, 158, 11, 0.15)' : 'rgba(16, 185, 129, 0.15)'}; color: ${driftColor}; border: 1px solid ${driftColor}33; padding: 2px 6px; border-radius: 4px; font-size: 10px; font-weight: bold; text-transform: uppercase; letter-spacing: 0.05em; display: inline-flex; align-items: center; gap: 4px;">
                <span style="display: inline-block; width: 5px; height: 5px; border-radius: 50%; background-color: ${driftColor};"></span>
                ${driftText}
              </span>
            </td>
          </tr>
        </table>
      ` : `
        <div style="font-weight: bold; font-size: 13px; color: #ffffff; margin-bottom: 8px; border-bottom: 1px solid rgba(255, 255, 255, 0.1); padding-bottom: 6px; font-family: sans-serif;">
          ${d.package} <span style="font-weight: normal; color: #9ca3af; font-size: 11px;">(${d.constraint})</span>
        </div>
        <table style="border-collapse: collapse; width: 100%; font-size: 11px; font-family: monospace;">
          <tr>
            <td style="color: #9ca3af; padding: 2px 8px 2px 0; text-align: left;">Pinned:</td>
            <td style="color: #6366f1; font-weight: bold; padding: 2px 0;">${d.current_pinned_version || 'N/A'}</td>
          </tr>
          <tr>
            <td style="color: #9ca3af; padding: 2px 8px 2px 0; text-align: left;">First Seen:</td>
            <td style="color: #e2e8f0; padding: 2px 0;">${d.first_seen_version || 'N/A'}</td>
          </tr>
          <tr>
            <td style="color: #9ca3af; padding: 2px 8px 2px 0; text-align: left;">Latest Upstream:</td>
            <td style="color: #a855f7; font-weight: bold; padding: 2px 0;">🔒 Premium Feature</td>
          </tr>
          <tr>
            <td style="color: #9ca3af; padding: 2px 8px 2px 0; text-align: left;">Allowed Range:</td>
            <td style="color: #cbd5e1; padding: 2px 0;">${d.min_version || '0.0.0'} to ${d.max_version || '∞'}</td>
          </tr>
          <tr>
            <td style="color: #9ca3af; padding: 6px 8px 2px 0; text-align: left;">Status:</td>
            <td style="padding: 6px 0;">
              <span style="background-color: rgba(139, 92, 246, 0.15); color: #a855f7; border: 1px solid rgba(139, 92, 246, 0.3); padding: 2px 6px; border-radius: 4px; font-size: 10px; font-weight: bold; text-transform: uppercase; letter-spacing: 0.05em; display: inline-flex; align-items: center; gap: 4px;">
                <span style="display: inline-block; width: 5px; height: 5px; border-radius: 50%; background-color: #a855f7;"></span>
                Restricted (Standard Account)
              </span>
            </td>
          </tr>
        </table>
      `;

      g.append("rect")
        .attr("x", bodyMin)
        .attr("y", y - 5)
        .attr("width", bodyWidth)
        .attr("height", 10)
        .attr("fill", bodyColor)
        .attr("opacity", 0.4)
        .attr("rx", 2)
        .style("cursor", "pointer")
        .style("transition", "opacity 0.2s ease, stroke 0.2s ease")
        .on("mouseover", function(event) {
          d3.select(this)
            .attr("opacity", 0.85)
            .attr("stroke", "#ffffff")
            .attr("stroke-width", 1);
          
          tooltip
            .style("visibility", "visible")
            .style("opacity", "1")
            .style("transform", "translateY(0px)")
            .html(tooltipHtml);
        })
        .on("mousemove", function(event) {
          const tooltipWidth = 260;
          const tooltipHeight = 150;
          let left = event.pageX + 15;
          let top = event.pageY - 10;
          
          if (left + tooltipWidth > window.innerWidth + window.scrollX) {
            left = event.pageX - tooltipWidth - 15;
          }
          if (top + tooltipHeight > window.innerHeight + window.scrollY) {
            top = event.pageY - tooltipHeight - 15;
          }
          
          tooltip
            .style("top", top + "px")
            .style("left", left + "px");
        })
        .on("mouseleave", function() {
          d3.select(this)
            .attr("opacity", 0.4)
            .attr("stroke", "none");
          
          tooltip
            .style("visibility", "hidden")
            .style("opacity", "0")
            .style("transform", "translateY(10px)");
        });

      const xPinned = xScale(pinnedVal);
      g.append("circle")
        .attr("cx", xPinned)
        .attr("cy", y)
        .attr("r", 5)
        .attr("fill", "#6366f1")
        .attr("stroke", "#ffffff")
        .attr("stroke-width", 1.5);

      g.append("text")
        .attr("x", xPinned)
        .attr("y", y - 10)
        .attr("text-anchor", "middle")
        .attr("fill", "#c7d2fe")
        .attr("font-size", "11px")
        .attr("font-family", "monospace")
        .attr("font-weight", "bold")
        .text(d.current_pinned_version);

      g.append("text")
        .attr("x", xMin)
        .attr("y", y + 18)
        .attr("text-anchor", "middle")
        .attr("fill", "#cbd5e1")
        .attr("font-size", "10px")
        .attr("font-family", "monospace")
        .text(d.min_version);

      if (!isInfinity) {
        g.append("text")
          .attr("x", xMax)
          .attr("y", y + 18)
          .attr("text-anchor", "middle")
          .attr("fill", "#cbd5e1")
          .attr("font-size", "10px")
          .attr("font-family", "monospace")
          .text(d.max_version);
      }

      if (isPremium && latestVal !== pinnedVal) {
        g.append("text")
          .attr("x", xLatest)
          .attr("y", y + 18)
          .attr("text-anchor", "middle")
          .attr("fill", "#ffffff")
          .attr("font-size", "11px")
          .attr("font-weight", "bold")
          .attr("font-family", "monospace")
          .text(d.latest_upstream_version);

        g.append("line")
          .attr("x1", xLatest)
          .attr("y1", y - 8)
          .attr("x2", xLatest)
          .attr("y2", y + 8)
          .attr("stroke", "#f59e0b")
          .attr("stroke-width", 1.5);
      }
    });
  }
</script>
